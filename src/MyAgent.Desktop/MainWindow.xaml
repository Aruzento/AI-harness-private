<Window x:Class="MyAgent.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI Harness v0.1.2"
        Height="700"
        Width="900"
        MinHeight="450"
        MinWidth="600">

    <Grid Margin="16">

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox
            x:Name="ConversationTextBox"
            Grid.Row="0"
            Margin="0,0,0,12"
            Padding="12"
            IsReadOnly="True"
            AcceptsReturn="True"
            TextWrapping="Wrap"
            VerticalScrollBarVisibility="Auto" />

        <Grid Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox
                x:Name="InputTextBox"
                Grid.Column="0"
                MinHeight="80"
                MaxHeight="180"
                Margin="0,0,12,0"
                Padding="8"
                AcceptsReturn="True"
                TextWrapping="Wrap"
                VerticalScrollBarVisibility="Auto" />

            <Button
                x:Name="SendButton"
                Grid.Column="1"
                Width="110"
                Height="40"
                VerticalAlignment="Bottom"
                Content="Отправить"
                Click="SendButton_Click" />

        </Grid>

    </Grid>

</Window>